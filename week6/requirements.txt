# MCP SDK — pinned to v1.x on purpose.
# `pip install mcp` now resolves to 2.x, where FastMCP was RENAMED to MCPServer
# and `mcp.server.fastmcp` no longer exists. This pin keeps FastMCP available.
mcp>=1.28,<2

# LLM client
anthropic>=0.40

# Validates tool arguments (constraints are declared on the tool parameters)
pydantic>=2.0

# Loads the .env file automatically
python-dotenv>=1.0
