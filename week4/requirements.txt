# LLM client (required)
anthropic>=0.40

# Validation + schema generation (required for this week)
pydantic>=2.0

# Vector search + math (required)
faiss-cpu>=1.7
numpy>=1.24

# Used to call the local Ollama embedding server (required)
requests>=2.31

# Optional: loads the .env file automatically
python-dotenv>=1.0
